# Workflow Template

The full workflow is shipped at `conductor/workflow.md`. This templates directory exists
for the `conductor-setup` skill which copies code style guides from here.

To add language-specific style guides, create files in:
`conductor/templates/code_styleguides/<language>.md`
